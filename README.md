Drone Flight-Time Calculator

AI-Use Disclosure

Used GitHub Copilot (inline completions) to draft the logic for calculate_flight_time() and flight_time_table(). Rewrote a few parts to handle negative values and the zero-floor case correctly. Verified the function behavior and error handling by running pytest after the edits.